class Stack():

    def __init__(self):
        self.stack = []
    
    def pop(self):
        if(len(self.stack) != 0):
            stack.pop()
        return None

    def peek(stack):
        if(len(stack) >0):
            return stack[len(stack)-1]
        return None
    
    def push(self, item):
        self.stack.append(item)
        return stack
    def size(self):
        return len(self.stack)
    
stack = Stack()

stack.push(stack, 2)









